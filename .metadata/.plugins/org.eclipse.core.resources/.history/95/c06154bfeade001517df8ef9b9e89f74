<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.his.dao.downpublic.SurgeryResultDao" >
  <resultMap id="BaseResultMap" type="com.his.entity.downpublic.SurgeryResult" >
    <id column="ResultID" property="resultid" jdbcType="CHAR" />
    <result column="ResultDescripting" property="resultdescripting" jdbcType="NVARCHAR" />
    
    <association column="ApplyID" property="surgeryapply"
			select="com.his.dao.downpublic.SurgeryApplyDao.selectByPrimaryKey" />
  </resultMap>
  
  <sql id="Base_Column_List" >
    ResultID, ResultDescripting, ApplyID
  </sql>
  
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.String" >
    select 
    <include refid="Base_Column_List" />
    from SurgeryResult
    where ResultID = #{resultid,jdbcType=CHAR}
  </select>
  
  <select id="selectByMap" parameterType="Map" resultMap="BaseResultMap">
		select * from SurgeryResult
		<where>
			<if test="resultid !=null and resultid !=''">
				and ResultID = #{resultid}
			</if>
		</where>
	</select>
  
  <delete id="deleteByPrimaryKey" parameterType="java.lang.String" >
    delete from SurgeryResult
    where ResultID = #{resultid,jdbcType=CHAR}
  </delete>
  
  <delete id="deleteByMap" parameterType="Map">
		delete from SurgeryResult
		<where>
		    <if test="resultid !=null and resultid !=''">
				and ResultID = #{resultid}
			</if>
			<if test="applyid !=null and applyid !=''">
				and ApplyID = #{applyid}
			</if>
		</where>
	</delete>
	
  <insert id="insertSelective" parameterType="com.his.entity.downpublic.SurgeryResult" >
    insert into SurgeryResult
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="resultid != null" >
        ResultID,
      </if>
      <if test="resultdescripting != null" >
        ResultDescripting,
      </if>
      <if test="surgeryapply != null" >
        ApplyID,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="resultid != null" >
        #{resultid,jdbcType=CHAR},
      </if>
      <if test="resultdescripting != null" >
        #{resultdescripting,jdbcType=NVARCHAR},
      </if>
      <if test="surgeryapply != null" >
        #{surgeryapply.applyid,jdbcType=CHAR},
      </if>
    </trim>
  </insert>
  
  <update id="updateByPrimaryKeySelective" parameterType="com.his.entity.downpublic.SurgeryResult" >
    update SurgeryResult
    <set >
      <if test="resultdescripting != null" >
        ResultDescripting = #{resultdescripting,jdbcType=NVARCHAR},
      </if>
      <if test="surgeryapply != null" >
        ApplyID = #{surgeryapply.applyid,jdbcType=CHAR},
      </if>
    </set>
    where ResultID = #{resultid,jdbcType=CHAR}
  </update>
  
  <select id="findSurgeryResult" parameterType="Map" resultMap="BaseResultMap">
		select * from SurgeryResult
		<where>
			<if test="resultid !=null and resultid !=''">
				and ResultID = #{resultid}
			</if>
			<if test="applyid !=null and applyid !=''">
				and ApplyID = #{applyid}
			</if>
		</where>
		<if test="start!=null and size!=null">
			order by ResultID offset ${start} rows fetch next
			${size} rows only
		</if>
	</select>

	<select id="getTotalSurgeryResult" parameterType="Map"
		resultType="Long">
		select count(*) from SurgeryResult
		<where>
			<if test="resultid !=null and resultid !=''">
				and ResultID = #{resultid}
			</if>
			<if test="applyid !=null and applyid !=''">
				and ApplyID = #{applyid}
			</if>
		</where>
	</select>

	<select id="getMaxID" parameterType="Map" resultType="String">
		select MAX(ResultID) from SurgeryResult
		<where>
			<!-- ApplyID -->
			<if test="prefix!=null and prefix!='' ">
				and ResultID like #{prefix}
			</if>
		</where>
	</select>
</mapper>